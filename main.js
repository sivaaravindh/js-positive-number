
/*let num = parseInt (prompt("Enter the value"));

let sum = 0;

for (var i = 0; i < num; ++i) {
    if (num> 0) {
        
    }
}*/

/*let a;
let add=0;
for (let x=0; x<=a; x++) {

    a=parseInt (prompt("Enter the value"));
    for (let j=1; j<=a; j++){

    if (x >0) {
            console.log(x +  " is even");
    }
    
    add += x;
    }
    document.write("The sum of positive numbers" + add)
}
var numbers = [1, 2, 3, -4, -5, -6];

var sum = 0;

for (var i = 0; i < numbers.length; ++i) {
    if (numbers[i] > 0) {
        sum += numbers[i];
    }
}

console.log(sum);

const posnum=parseInt (prompt("Enter the value"));
    let sum = 0;
    for (let i = 0; i < posnum; ++i) {
        if (i > 0) {
            sum += posnum;
        }
    }
    console.log(sum);*/

    let num=parseInt (prompt("Enter the value"));
    let sum= 0;
    for(let i=1; i<=num; i++){
            let b=parseInt (prompt("Enter the value"));

            if(b>0){
                sum +=b;
            }
    }
    
    document.write(sum);



  
   
    


